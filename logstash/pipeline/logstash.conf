input {
	beats {
		port => 5044
	}

	tcp {
		port => 50000
		codec => json_lines
	}
}

## Add your filters / logstash plugins configuration here

output {
	# 仅测试使用(在控制台输出)
	#stdout {
  #     codec => rubydebug
	#}
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "logstash_internal"
		password => "${LOGSTASH_INTERNAL_PASSWORD}"
		index => "logstash-sls-%{+YYYY.MM}" # 使用 index 则会对每个key产生一个keyword(todo)
	}
}
